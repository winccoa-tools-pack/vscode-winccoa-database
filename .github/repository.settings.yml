# Repository settings applied via GitHub REST API by .github/workflows/apply-settings-and-rulesets.yml
# This repo uses GitFlow: develop is default, main is stable release.

description: "A VS Code extension that recreates the WinCC OA PARA module, providing a graphical interface for browsing and editing datapoint types (DPTs), datapoints (DPs), and their configurations directly from VS Code."
homepage: "https://github.com/winccoa-tools-pack"
default_branch: "develop"

has_issues: true
has_wiki: false

allow_squash_merge: true
allow_merge_commit: true
allow_rebase_merge: false

delete_branch_on_merge: true

topics:
  - winccoa
  - wincc-oa
  - vscode
  - automation
  - vscode-extension
  - vscode-extension-pack
  - scada
  - hmi
  - industrial-automation
  - Siemens
  - ETM

security:
  enable_vulnerability_alerts: true
  enable_automated_security_fixes: true
